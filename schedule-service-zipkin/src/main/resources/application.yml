server:
  port: 9090
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: '*'
#spring.cloud.config.profile: dev
#spring.cloud.config.discovery.serviceId: config-server