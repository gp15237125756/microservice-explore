spring:
  application:
    name: web-gateway
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
  sleuth:
    traceId128: true
    sampler:
      probability: 1.0
encrypt:
  failOnError: false